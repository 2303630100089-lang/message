apiVersion: apps/v1
kind: Deployment
metadata:
  name: pulsesphere-gateway
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pulsesphere-gateway
  template:
    metadata:
      labels:
        app: pulsesphere-gateway
    spec:
      containers:
        - name: gateway
          image: pulsesphere/gateway:latest
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: pulsesphere-gateway
spec:
  selector:
    app: pulsesphere-gateway
  ports:
    - port: 80
      targetPort: 8080
