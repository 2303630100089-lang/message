# PulseSphere Android

Jetpack Compose app with modular architecture (core + feature modules).

## Modules
- app
- core/common, core/designsystem, core/data, core/domain
- feature/auth, feature/chat, feature/feed, feature/profile

## Build (requires Android SDK)
```
./gradlew :app:assembleDebug
```
